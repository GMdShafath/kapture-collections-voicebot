# Port the mock webhook server listens on
PORT=3000

# Optional: shared secret to validate incoming Vapi webhook requests
# (add validation middleware in server.js if using this in production)
VAPI_WEBHOOK_SECRET=your_shared_secret_here
